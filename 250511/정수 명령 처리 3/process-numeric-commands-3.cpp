#include <iostream>
#include <string>

using namespace std;

int N;
string cmd[10000];
int num[10000];

int main() {
    cin >> N;
    for (int i = 0; i < N; i++) {
        cin >> cmd[i];
        if (cmd[i] == "push_front" || cmd[i] == "push_back") {
            cin >> num[i];
        }
        if (cmd[i] == "push_front" || cmd[i] == "push_back") {
            cin >> num[i];
        }
        if (cmd[i] == "push_front" || cmd[i] == "push_back") {
            cin >> num[i];
        }
        if (cmd[i] == "push_front" || cmd[i] == "push_back") {
            cin >> num[i];
        }
        if (cmd[i] == "push_front" || cmd[i] == "push_back") {
            cin >> num[i];
        }
        if (cmd[i] == "push_front" || cmd[i] == "push_back") {
            cin >> num[i];
        }
    }

    // Please write your code here.

    return 0;
}
